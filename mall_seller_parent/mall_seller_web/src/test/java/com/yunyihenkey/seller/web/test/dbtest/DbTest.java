/*
package com.yunyihenkey.seller.web.LongUtil.dbtest;

import java.util.List;
import java.util.Map;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.LongUtil.context.SpringBootTest;
import org.springframework.LongUtil.context.junit4.SpringRunner;

import com.github.pagehelper.PageHelper;
import com.yunyihenkey.Application;
import com.yunyihenkey.basedao.malldb.commonMapper.MallSnowflakeDatacenterMapper;

@RunWith(SpringRunner.class)
@SpringBootTest(classes = Application.class)
// @ActiveProfiles("uat")
public class DbTest {

	*/
/**
	 *
	 *//*

	@Autowired
	private MallSnowflakeDatacenterMapper mallSnowflakeDatacenterMapper;

	@Test
	public void LongUtil() {

//		PageHelper.startPage(1, 3,"role_id desc");
		PageHelper.startPage(1, 10);
		List<Map<String, Object>> LongUtil = mallSnowflakeDatacenterMapper.LongUtil();

		System.out.println(LongUtil);
	}

}
*/
